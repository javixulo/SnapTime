# SnapTime — Copilot Instructions

AI behavior rules for this project are defined in `AGENTS.md` (repo root). That file is the single source of truth.

Refer to `AGENTS.md` for mandatory rules about commits, documentation, dependencies, and architecture changes.
